# sync-md assets

Public image assets used in the sync.md VS Code extension's README (kept in a
separate public repo so the main extension source can remain private, while
Marketplace screenshot images still resolve to public https:// URLs).
